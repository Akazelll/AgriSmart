# --- Supabase Configuration ---
# Dapatkan URL dan Anon Key dari Project Settings > API di Dashboard Supabase
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# Service Role Key diperlukan untuk bypass Row Level Security (RLS) pada Server Actions
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"
SUPABASE_URL="https://your-project-id.supabase.co"

# --- NextAuth Configuration ---
# Gunakan perintah 'openssl rand -base64 32' di terminal untuk membuat secret yang aman
AUTH_SECRET="your-generated-auth-secret"

# Konfigurasi Google OAuth
AUTH_GOOGLE_ID="your-google-oauth-client-id"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# --- AI Backend API ---
# URL ke server Flask yang menjalankan model EfficientNet
# Gunakan http://127.0.0.1:8000 jika menjalankan backend Python secara lokal
NEXT_PUBLIC_API_URL="your-flask-backend-url"

# --- Weather API (Optional/External) ---
# Jika Anda menggunakan provider cuaca spesifik, tambahkan API key di sini
NEXT_PUBLIC_WEATHER_API_KEY="your-api-key"